server:
  port: 8090

spring:
  application:
    name: wcafe-api-gateway
  cloud:
    config:
      uri: '${config.server:http://localhost:8888/}'
      name: common-config, db-config, eureka-config, zuul-config
